pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // https://github.com/rovo89/XposedBridge/issues/288
        maven { url 'https://api.xposed.info/' }
    }
}
rootProject.name = "Telegram_Story_Hook"
include ':app'
